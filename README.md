bug-groovy-2.3-override
=======================

A reference project to show that an interface implementation that implements (overrides) a method with a generics parameter won't compile when it is assigned a default value. 
